import React from "react";

import styles from './NowPlayingBar.module.css'

const NowPlayingBar:React.FC = () => {
    return (
        <div className={`w-full h-[5.625rem] ${styles.wrapper}`}>
            asdasda
        </div>
    );
};

export default NowPlayingBar;